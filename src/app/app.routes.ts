import { Route } from '@angular/router';
import { SingalListComponent } from './singal-list/singal-list.component';

export const appRoutes: Route[] = [{
  path: '',
  component: SingalListComponent,
}];
